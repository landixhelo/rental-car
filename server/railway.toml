[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Schema already applied; start API directly so logs show boot clearly.
startCommand = "node dist/index.js"
restartPolicyType = "ON_FAILURE"

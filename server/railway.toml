[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Start command comes from Dockerfile CMD (includes prisma db push + node).
# Do not override with a shell-less `&&` command — Railway will fail to boot.
restartPolicyType = "ON_FAILURE"

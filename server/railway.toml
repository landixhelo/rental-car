[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Apply schema then start API
startCommand = "npx prisma db push --skip-generate && node dist/index.js"
restartPolicyType = "ON_FAILURE"

# Production frontend talks to API via same-origin `/api` (Vercel rewrite → Railway).
# Leave VITE_API_URL empty/unset on Vercel so auth cookies work on mobile.
# Local override example:
# VITE_API_URL=http://localhost:5000
